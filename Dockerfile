# Используем легкий веб-сервер nginx
FROM nginx:alpine

# Копируем все файлы проекта в папку сервера
COPY . /usr/share/nginx/html

# Открываем порт 80 (HTTP)
EXPOSE 80

# Запускаем сервер (команда уже встроена в образ nginx)